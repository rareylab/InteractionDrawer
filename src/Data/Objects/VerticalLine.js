/**
 * Class representing a vertical line at some x-position.
 */
class VerticalLine {
    /**
     * Create a new VerticalLine object.
     *
     * @param x - x-position of the line
     */
    constructor(x) {
        this.x = x;
    }
}